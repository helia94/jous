# SEO status report — 2026-08-31 (baseline)

Property: `https://jous.app/` (URL-prefix, owned by service account `jous-881@search-1753904394656.iam.gserviceaccount.com` + helia.jm@gmail.com). The domain property `sc-domain:jous.app` exists in the owner's account but the service account is not a user on it yet.

## What changed today
- Every URL used to serve the same empty React shell. Now 12 conversation-card pages are pre-rendered at build time (real title/h1/canonical/FAQ JSON-LD), `robots.txt` exists, `www.` 301s to the apex, sitemap cleaned (477 URLs) and submitted via the API.

## Index status (URL Inspection API)
| URL | Verdict | Coverage | Last crawl |
|---|---|---|---|
| https://jous.app/ | PASS | Submitted and indexed | 2026-08-28 |
| https://jous.app/conversation-cards | PASS | Submitted and indexed | 2026-08-12 |
| https://jous.app/free-conversation-cards | NEUTRAL | Discovered – currently not indexed | never |
| https://jous.app/blog/art-of-small-talk | NEUTRAL | Discovered – currently not indexed | never |

## Search analytics (2026-06-01 → 2026-08-29)
No rows returned — property added today; data normally appears within 1–3 days.

## Next checks
- Do the "Discovered – not indexed" spoke pages get crawled and indexed now that they serve real HTML?
- First impressions/clicks for queries containing "conversation cards".
- Blog posts: 460 URLs still client-rendered (content is in the DB) — next technical lever.

## Weekly SEO monitor run — 2026-08-31 (later same day) — FAILED: missing credentials

The scheduled weekly SEO monitor ran later on 2026-08-31 and could not fetch any Search Console data.

`GOOGLE_SERVICE_ACCOUNT_JSON` was not set in this session's environment, and no fallback credential (`GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_REFRESH_TOKEN`, `GOOGLE_ACCESS_TOKEN`) was present either. Running the report script produced:

```
Search Console report failed: Missing auth. Set GOOGLE_SERVICE_ACCOUNT_JSON, GOOGLE_APPLICATION_CREDENTIALS, GOOGLE_REFRESH_TOKEN credentials, or GOOGLE_ACCESS_TOKEN.
```

No search analytics, no URL inspections, and no site list could be retrieved this run. The baseline data above (from earlier today) is unaffected and still stands as the last known-good report. This existing file was kept as-is (not overwritten) rather than replaced, since it already carries today's date and contains real baseline data — this run only appends this note.

**Action needed from the owner:** re-provision `GOOGLE_SERVICE_ACCOUNT_JSON` (or another supported credential) in the environment this weekly routine runs in, so next week's run can pull real data.

## Update (evening): second wave shipped
- `/question/<id>` pages (1,676 live questions) are now server-rendered with the question text as `<title>`/`<h1>`, public answers (QAPage JSON-LD), related cards, and `?lang=de|es|fa` variants with hreflang. Listed in the new `/sitemap-questions.xml` (submitted to Google, accepted by IndexNow).
- `/blog/<url>` (460 posts) server-rendered from the database with Article JSON-LD.
- Six localized landing pages (DE/ES/FA) with hreflang groups, deploying.
- IndexNow set up (key served at `/615228870f61f6b6892de92d84f56b1a.txt`); 476 + 1,676 URLs submitted → Bing/DuckDuckGo/Yandex/Naver.
- Search Console: five broken 2024 "sitemap" entries deleted; `sitemap.xml` + `sitemap-questions.xml` remain.
- GitHub repo: homepage/description/topics set, README rewritten with deep links.
- Ruled out by owner: Product Hunt, Reddit. Outreach plan rewritten (`docs/seo-outreach.md`).

## Watch next week
- Index status of `/question/*` sample, `/blog/*` sample, and the six localized pages.
- Whether Google picks `?lang=` variants as duplicates (hreflang + self-canonical should prevent it); if it does, switch translations to path-based URLs.
- First impressions for German/Spanish/Persian queries.

## Weekly SEO monitor run — 2026-08-31 (later run) — FAILED: could not reach jous.app

This scheduled run attempted to fetch `https://jous.app/api/seo/weekly?inspect=1` (the site's own read-only SEO reporting endpoint, per the token-protected route added this week). Both the initial request and the retry were blocked before reaching the site at all — by this session's own outbound egress proxy, not by the jous.app server.

Exact failure, identical on both attempts:

```
HTTP/1.1 403 Forbidden
Content-Type: text/plain; charset=utf-8
X-Content-Type-Options: nosniff
Content-Length: 66
Connection: close

[agent-proxy] connect_rejected: the egress proxy denied the CONNECT to
jous.app:443 (organization policy or could not reach the destination)
```

curl itself reported exit code 1 / HTTP status `000` (no TLS connection was ever established to jous.app; the 403 above came from the local agent-proxy gateway, confirmed via `curl http://127.0.0.1:43331/__agentproxy/status`, which listed `jous.app:443` under `recentRelayFailures` with `kind: connect_rejected`).

No analytics, index inspections, or query/page data could be retrieved this run. Nothing in this file above this section was touched. This looks like an environment/network-policy issue (this session's egress allowlist does not currently permit `jous.app`) rather than an application or credentials problem — no code change is warranted.

**Action needed from the owner:** allow outbound HTTPS to `jous.app` from this scheduled routine's execution environment (egress/network policy), so next week's run can reach the reporting endpoint.
