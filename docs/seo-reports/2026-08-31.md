# SEO status report — 2026-08-31 (baseline)

Property: `https://jous.app/` (URL-prefix, owned by service account `jous-881@search-1753904394656.iam.gserviceaccount.com` + helia.jm@gmail.com). The domain property `sc-domain:jous.app` exists in the owner's account but the service account is not a user on it yet.

## What changed today
- Every URL used to serve the same empty React shell. Now 12 conversation-card pages are pre-rendered at build time (real title/h1/canonical/FAQ JSON-LD), `robots.txt` exists, `www.` 301s to the apex, sitemap cleaned (477 URLs) and submitted via the API.

## Index status (URL Inspection API)
| URL | Verdict | Coverage | Last crawl |
|---|---|---|---|
| https://jous.app/ | PASS | Submitted and indexed | 2026-08-28 |
| https://jous.app/conversation-cards | PASS | Submitted and indexed | 2026-08-12 |
| https://jous.app/free-conversation-cards | NEUTRAL | Discovered – currently not indexed | never |
| https://jous.app/blog/art-of-small-talk | NEUTRAL | Discovered – currently not indexed | never |

## Search analytics (2026-06-01 → 2026-08-29)
No rows returned — property added today; data normally appears within 1–3 days.

## Next checks
- Do the "Discovered – not indexed" spoke pages get crawled and indexed now that they serve real HTML?
- First impressions/clicks for queries containing "conversation cards".
- Blog posts: 460 URLs still client-rendered (content is in the DB) — next technical lever.

## Weekly SEO monitor run — 2026-08-31 (later same day) — FAILED: missing credentials

The scheduled weekly SEO monitor ran later on 2026-08-31 and could not fetch any Search Console data.

`GOOGLE_SERVICE_ACCOUNT_JSON` was not set in this session's environment, and no fallback credential (`GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_REFRESH_TOKEN`, `GOOGLE_ACCESS_TOKEN`) was present either. Running the report script produced:

```
Search Console report failed: Missing auth. Set GOOGLE_SERVICE_ACCOUNT_JSON, GOOGLE_APPLICATION_CREDENTIALS, GOOGLE_REFRESH_TOKEN credentials, or GOOGLE_ACCESS_TOKEN.
```

No search analytics, no URL inspections, and no site list could be retrieved this run. The baseline data above (from earlier today) is unaffected and still stands as the last known-good report. This existing file was kept as-is (not overwritten) rather than replaced, since it already carries today's date and contains real baseline data — this run only appends this note.

**Action needed from the owner:** re-provision `GOOGLE_SERVICE_ACCOUNT_JSON` (or another supported credential) in the environment this weekly routine runs in, so next week's run can pull real data.

## Update (evening): second wave shipped
- `/question/<id>` pages (1,676 live questions) are now server-rendered with the question text as `<title>`/`<h1>`, public answers (QAPage JSON-LD), related cards, and `?lang=de|es|fa` variants with hreflang. Listed in the new `/sitemap-questions.xml` (submitted to Google, accepted by IndexNow).
- `/blog/<url>` (460 posts) server-rendered from the database with Article JSON-LD.
- Six localized landing pages (DE/ES/FA) with hreflang groups, deploying.
- IndexNow set up (key served at `/615228870f61f6b6892de92d84f56b1a.txt`); 476 + 1,676 URLs submitted → Bing/DuckDuckGo/Yandex/Naver.
- Search Console: five broken 2024 "sitemap" entries deleted; `sitemap.xml` + `sitemap-questions.xml` remain.
- GitHub repo: homepage/description/topics set, README rewritten with deep links.
- Ruled out by owner: Product Hunt, Reddit. Outreach plan rewritten (`docs/seo-outreach.md`).

## Watch next week
- Index status of `/question/*` sample, `/blog/*` sample, and the six localized pages.
- Whether Google picks `?lang=` variants as duplicates (hreflang + self-canonical should prevent it); if it does, switch translations to path-based URLs.
- First impressions for German/Spanish/Persian queries.

## Weekly SEO monitor run — 2026-08-31 (later run) — FAILED: could not reach jous.app

This scheduled run attempted to fetch `https://jous.app/api/seo/weekly?inspect=1` (the site's own read-only SEO reporting endpoint, per the token-protected route added this week). Both the initial request and the retry were blocked before reaching the site at all — by this session's own outbound egress proxy, not by the jous.app server.

Exact failure, identical on both attempts:

```
HTTP/1.1 403 Forbidden
Content-Type: text/plain; charset=utf-8
X-Content-Type-Options: nosniff
Content-Length: 66
Connection: close

[agent-proxy] connect_rejected: the egress proxy denied the CONNECT to
jous.app:443 (organization policy or could not reach the destination)
```

curl itself reported exit code 1 / HTTP status `000` (no TLS connection was ever established to jous.app; the 403 above came from the local agent-proxy gateway, confirmed via `curl http://127.0.0.1:43331/__agentproxy/status`, which listed `jous.app:443` under `recentRelayFailures` with `kind: connect_rejected`).

No analytics, index inspections, or query/page data could be retrieved this run. Nothing in this file above this section was touched. This looks like an environment/network-policy issue (this session's egress allowlist does not currently permit `jous.app`) rather than an application or credentials problem — no code change is warranted.

**Action needed from the owner:** allow outbound HTTPS to `jous.app` from this scheduled routine's execution environment (egress/network policy), so next week's run can reach the reporting endpoint.

## Weekly SEO monitor run — 2026-08-31 (later run) — FAILED: endpoint returned HTTP 500

This scheduled run reached `https://jous.app/api/seo/weekly?inspect=1` successfully this time — the previous egress block is resolved (no proxy relay failures, TLS/HTTP2 connection to jous.app completed normally). However, the endpoint itself returned an internal server error on both the initial request and the one retry (30s apart, per the run's error-handling instructions).

Exact response, identical on both attempts:

```
HTTP/2 500
content-type: text/html
server: railway-hikari
content-length: 141

<html>
  <head>
    <title>Internal Server Error</title>
  </head>
  <body>
    <h1><p>Internal Server Error</p></h1>

  </body>
</html>
```

Both requests took ~30 seconds before returning the error (the endpoint is documented as able to take up to ~60s), suggesting the handler is running and failing partway through — e.g. while calling out to the Search Console API — rather than failing instantly on a routing or auth problem. The generic framework error page (no stack trace or JSON error body) means the exact cause can't be determined from the client side; this needs to be checked from server-side logs.

No search analytics, index inspections, or page/query data could be retrieved this run. Nothing above this section was touched.

**Action needed from the owner:** check the `/api/seo/weekly` route's server logs on Railway for the exception behind this 500 (likely a Search Console API auth/credential issue, given the ~30s hang before failing) and fix it so next week's run can pull real data.

## Weekly SEO monitor run — 2026-08-31 (later run) — SUCCESS: first real data pull

This run reached `https://jous.app/api/seo/weekly?inspect=1` and got a clean `200` on the first attempt (no retry needed). This is the first time this monitor has successfully pulled analytics + inspection data in one call — the prior three runs today failed on missing credentials, egress block, and a 500 respectively. Window returned: 7d = 2026-08-22 → 2026-08-28, 28d = 2026-08-01 → 2026-08-28.

Baseline used for comparison: the "baseline" section at the top of this same file (the only prior report), which listed 4 inspected URLs and no analytics rows.

### 1. Headline numbers
| Window | Clicks | Impressions | Avg. position | vs. previous report |
|---|---|---|---|---|
| 7d (2026-08-22 → 2026-08-28) | 0 | 0 | N/A (no rows) | unchanged — baseline also had 0 rows |
| 28d (2026-08-01 → 2026-08-28) | 0 | 0 | N/A (no rows) | unchanged — baseline also had 0 rows |

All five analytics tables (`7d_queries`, `7d_pages`, `28d_queries`, `28d_pages`, `28d_conversation_queries`) came back with **zero rows**. Search Console is still reporting no query/page performance data for this property at all, even though several pages are confirmed indexed (see below) and the baseline note said data "normally appears within 1–3 days."

### 2. Index status (URL Inspection API) — 19 URLs inspected
| URL | Verdict | Coverage | Last crawl | Changed since last report |
|---|---|---|---|---|
| https://jous.app/ | PASS | Submitted and indexed | 2026-08-31 | Re-crawled (was 2026-08-28) |
| https://jous.app/conversation-cards | PASS | Submitted and indexed | 2026-08-12 | No change |
| https://jous.app/weird-conversation-cards | PASS | Submitted and indexed | 2026-08-20 | New to this report (not previously inspected) |
| https://jous.app/blog | NEUTRAL | Crawled - currently not indexed | 2026-05-08 | New to this report |
| https://jous.app/blog/art-of-small-talk | NEUTRAL | URL is unknown to Google | never | Changed — was "Discovered – currently not indexed" |
| https://jous.app/free-conversation-cards | NEUTRAL | URL is unknown to Google | never | Changed — was "Discovered – currently not indexed" |
| https://jous.app/conversation-cards-for-couples | NEUTRAL | Discovered - currently not indexed | never | New to this report |
| https://jous.app/conversation-cards-for-date-night | NEUTRAL | URL is unknown to Google | never | New to this report |
| https://jous.app/conversation-cards-for-friends | NEUTRAL | URL is unknown to Google | never | New to this report |
| https://jous.app/de/gespraechskarten | NEUTRAL | Discovered - currently not indexed | never | New to this report |
| https://jous.app/es/cartas-de-conversacion | NEUTRAL | URL is unknown to Google | never | New to this report |
| https://jous.app/fa/conversation-cards | NEUTRAL | URL is unknown to Google | never | New to this report |
| https://jous.app/non-cringe-conversation-starters | NEUTRAL | Discovered - currently not indexed | never | New to this report |
| https://jous.app/online-conversation-cards | NEUTRAL | URL is unknown to Google | never | New to this report |
| https://jous.app/open-source-card-dataset | NEUTRAL | Discovered - currently not indexed | never | New to this report |
| https://jous.app/open-source-conversation-cards | NEUTRAL | URL is unknown to Google | never | New to this report |
| https://jous.app/printable-conversation-cards | NEUTRAL | URL is unknown to Google | never | New to this report |
| https://jous.app/question/1441 | NEUTRAL | Discovered - currently not indexed | never | New to this report |
| https://jous.app/random-conversation-cards | NEUTRAL | URL is unknown to Google | never | New to this report |

### 3. Top queries (28d) and top pages (28d)
No rows returned for either `28d_queries` or `28d_pages`. There is no query or page performance data to rank.

### 4. Queries containing "conversation" (28d)
No rows returned for `28d_conversation_queries`.

### 5. Opportunities (impressions ≥ 20 and CTR ≤ 2% or position ≥ 10)
Cannot be computed — no analytics rows (0 impressions across all pages/queries) were returned for the 28d window.

### 6. What changed since last week
- This is the first run that actually returned data end-to-end; the previous three attempts today failed on credentials, egress, and a server 500 in turn, so there was no comparable prior data pull to size a week-over-week trend against beyond the original baseline note.
- Search Console analytics are still completely empty (0 clicks, 0 impressions) across 7d and 28d, for every query and page breakdown, unchanged from the baseline's "no rows returned."
- Index coverage widened from 4 tracked URLs to 19: the homepage, `/conversation-cards`, and `/weird-conversation-cards` are confirmed `PASS` / "Submitted and indexed."
- Two URLs regressed in classification: `/blog/art-of-small-talk` and `/free-conversation-cards` now show "URL is unknown to Google," a step down from "Discovered – currently not indexed" in the baseline.
- Most of the newly-inspected URLs (localized pages, spoke pages, the sample question page) are either "URL is unknown to Google" or "Discovered - currently not indexed" — none of the newly added pages have been crawled yet.
- The homepage was re-crawled on 2026-08-31 (previously 2026-08-28), so Google is actively revisiting the site, just not yet indexing the long tail.

### 7. Recommended next actions
1. **Investigate why Search Console analytics are still at zero rows a month into the tracked window (28d = 2026-08-01 → 2026-08-28) despite 3 URLs showing "Submitted and indexed."** Indexed pages with literally 0 impressions for a full month is unusual and worth checking property ownership/verification and whether this is the correct property (URL-prefix `https://jous.app/` vs. the unclaimed domain property `sc-domain:jous.app` noted in the baseline) rather than assuming it will resolve itself.
2. **Request indexing for `/blog/art-of-small-talk` and `/free-conversation-cards`**, since both moved from "Discovered" to "URL is unknown to Google" — the opposite direction from progress — while the homepage was successfully re-crawled the same day.
3. **Submit or re-submit the 12 newly-inspected but uncrawled URLs** (localized `/de`, `/es`, `/fa` pages, the `conversation-cards-for-*` spokes, `/question/1441`, etc.) via URL Inspection "Request Indexing," since none of them show any crawl activity (`lastCrawl: null`) yet.
